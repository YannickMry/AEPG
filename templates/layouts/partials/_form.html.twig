{{form_start(form|default(form))}}
    {{form_widget(form|default(form))}}
    <button type="submit" class="mt-3 btn btn-primary">{{btn|default('Enregistrer')}}</button>
{{form_end(form|default(form))}}