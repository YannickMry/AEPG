<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{path('home')}}">
            <img src="{{asset('images/logo-aepg.png')}}" alt="Logo aepg" style="width: auto;height: 100px;">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav fs-5">
                <li class="nav-item">
                    <a class="nav-link {{app.request.get('_route') is same as('home') ? 'active'}}" href="{{path('home')}}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{app.request.get('_route') is same as('article_index') ? 'active'}}" href="{{path('article_index')}}">Actualités</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{app.request.get('_route') is same as('promotion_index') ? 'active'}}" href="{{path('promotion_index')}}">Promotions</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{app.request.get('_route') is same as('contact') ? 'active'}}" href="{{path('contact')}}">Contact</a>
                </li>
            </ul>
        </div>
    </div>
</nav>